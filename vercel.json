{
    "builds": [{
        "src": "music_api_project/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "music_api_project/wsgi.py"
        }
    ]
}